name: Build & Test

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  build:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v6

      - name: Select Xcode
        run: sudo xcode-select -s /Applications/Xcode_16.app

      - name: Build
        run: swift build

      - name: Test
        run: swift test

      - name: Verify .app bundle
        run: |
          BIN_PATH=$(swift build --show-bin-path)/SwapPointer
          APP_DIR="$RUNNER_TEMP/SwapPointer.app"
          CONTENTS_DIR="$APP_DIR/Contents"
          mkdir -p "$CONTENTS_DIR/MacOS"
          cp "$BIN_PATH" "$CONTENTS_DIR/MacOS/SwapPointer"
          cp Sources/SwapPointer/Resources/Info.plist "$CONTENTS_DIR/Info.plist"
          # Verify bundle structure
          test -f "$CONTENTS_DIR/MacOS/SwapPointer"
          test -f "$CONTENTS_DIR/Info.plist"
          /usr/libexec/PlistBuddy -c "Print :CFBundleIdentifier" "$CONTENTS_DIR/Info.plist"
